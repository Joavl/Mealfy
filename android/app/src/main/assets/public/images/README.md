# Imagens públicas

Coloque aqui a imagem de destaque (hero) da Home:

- **`home-hero.jpg`** — usada em `src/pages/Home.tsx` (`/images/home-hero.jpg`).
  Proporção recomendada: retrato (ex.: 1035×1200 ou similar), foco no rosto.
  Enquanto o arquivo não existir, a Home usa um fallback automático (Unsplash).
